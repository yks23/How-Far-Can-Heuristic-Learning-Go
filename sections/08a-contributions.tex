% ---------------------------------------------------------------------------
% Contribution roles, placed after the Conclusion at the user's direction
% (2026-08-19): "能不能在文末（也就是 Conclusion 后面）加上作者的排序？主要分为以下
% 四个部分：1. Core Contributor 2. Contributor 3. Corresponding Author：由 Kaisen
% Yang 担任 4. Project Lead".
%
% WHY THIS FILE IS SWITCHED OFF BY DEFAULT. The venue is DOUBLE-BLIND
% (NeurIPS.cc/2026/Workshop/CL4FMAgents, see the header of main.tex), and
% main.tex carries \author{Anonymous submission} under "no author, no
% affiliation, no acknowledgements until camera-ready". A named contribution
% block in the submitted PDF de-anonymises the paper to the reviewers, which is
% a desk-reject risk at most OpenReview venues and is not reversible once
% uploaded. So the block lives here, in the source, and renders only when
% \camerareadytrue is set in main.tex. Flip it at camera-ready (2026-10-10),
% not before, and flip it TOGETHER with \author -- a real \author line and an
% anonymous contribution block are as inconsistent as the reverse.
%
% NAMES ARE INCOMPLETE. Only the Corresponding Author was supplied. The other
% three groups are marked TODO rather than guessed: git history shows two
% committers (Tianxin Tech <txc@...>, 杨凯森/Kaisen Yang <yangkaisen39@gmail.com>),
% which is evidence about who edited the repo and NOT about authorship order or
% role -- inferring one from the other would put a name in a group its owner did
% not agree to. Ask before filling them in.
%
% ORDER WITHIN A GROUP. State the convention explicitly once names exist, since
% a bare list is ambiguous: either alphabetical-by-surname (and then say so) or
% contribution-ordered (and then the order is a claim each listed person has to
% have seen). Do not leave it unstated.
%
% FORMAT. \section*, so the block takes no section number and stays out of the
% numbered argument -- it is metadata about the paper, not a part of it. Roles
% are run-in \paragraph-style labels rather than a table, which survives a
% change of document class; the style file for this venue is still unconfirmed
% (main.tex header), so anything that depends on a class-specific environment
% would have to be rewritten when the class changes.
% ---------------------------------------------------------------------------

\section*{Contributions}

\noindent\textbf{Core Contributor.}
% TODO(names): not yet supplied. One or more names, with the intra-group
% ordering convention stated above.
\emph{Names to be supplied.}

\smallskip
\noindent\textbf{Contributor.}
% TODO(names): not yet supplied.
\emph{Names to be supplied.}

\smallskip
\noindent\textbf{Corresponding Author.}
Kaisen Yang.
% Supplied by the user. A corresponding author is normally given with a contact
% address; add the e-mail at camera-ready, when affiliations go in.

\smallskip
\noindent\textbf{Project Lead.}
% TODO(names): not yet supplied.
\emph{Names to be supplied.}
