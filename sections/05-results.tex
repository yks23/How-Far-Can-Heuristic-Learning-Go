% ---------------------------------------------------------------------------
% Results
%
% Four blocks, matching the four claims in the abstract.
%
% 1. The archive audit. \val{pheur}, \val{prl}, \val{nrlfinal}. Descriptive
%    shares over the archive; no counterfactual is claimed here.
%
% 2. HL within the human field. \val{pct} percentile, surpasses the strongest
%    recorded human on \val{nbeat}, \val{elogap} Elo. Percentile is the metric
%    the archive uniquely affords -- a gradient, not a single baseline -- so
%    lead with it rather than with percentage improvement over a reference.
%
% 3. Sample efficiency vs RL. \val{effratio} at matched Elo, crossover at
%    \val{crossover}. Frame as budget-conditional; do not claim HL dominates.
%    Show the axes where it does not.
%
% 4. The ceiling -- this is the section the title promises. Improvement
%    frequency decays as \val{decay}, plateau near \val{plateau} revisions,
%    mechanism \val{ceilingcause}. Fit the same way Frontier-Eng did (slope
%    constrained, report R^2) so the exponents are comparable; their single-agent
%    tasks give t^-1 with a plateau by 50-100 iterations. A different exponent
%    here is the more interesting outcome, but it needs a mechanism, not just a
%    curve: rule count growth, rules interfering with each other, or the agent
%    losing the ability to read its own program.
%
%    The mechanism claim is the load-bearing one and needs its own evidence, not
%    just a plateau. Program description length at the plateau, \val{maintcap},
%    compared across games: if it converges within \val{maintcapspread} the
%    ceiling is the agent's, if it tracks game complexity the ceiling is the
%    game's. State which outcome occurred before interpreting it -- the
%    convergence result is a prediction here, and reporting it as confirmed when
%    it is not is the single easiest way to lose the paper. If it fails, the
%    finding is still worth reporting; the framing changes, not the data.
%
%    Report the two halves of the prediction together, because each is weak
%    alone. The mechanism says the capacity belongs to the agent and K(pi_good) to
%    the game, so description length at the plateau should CONVERGE across games
%    (\val{maintcapspread}) while the Elo reached at that plateau should VARY with
%    the game (\val{eloplatspread}) -- high where a winning policy is short enough
%    to fit under the capacity, low where it is not. Convergent length with
%    convergent Elo would undercut the story as much as divergent length does: it
%    would suggest the games do not actually differ in K(pi_good), and the
%    cross-game comparison carries no information. So plot length-at-plateau and
%    Elo-at-plateau on the same set of games and say explicitly which of the four
%    combinations the data show.
%
%    Two supporting cuts, both cheap given the log: does program size at the
%    plateau move with model capability (the mechanism predicts it should, since
%    the bound is the maintainer's), and do the failed revisions near the plateau
%    fail by regression on already-working behaviour rather than by adding
%    nothing? The second is the direct signature of a maintenance limit and
%    distinguishes it from simple diminishing returns.
%
% 5. The archive's free experiment, which belongs to the benchmark rather than to
%    HL: for each Elo band, the shortest human program reaching it. Lower
%    envelope, not mean -- description length is a minimum. Slope \val{envslope},
%    knee at \val{envknee}. This is the only place the complexity axis is
%    measured on humans rather than on the agent, so it is what licenses ordering
%    games by K(pi_good) instead of by rule length. Same proxy as Section 3, and
%    say so.
% ---------------------------------------------------------------------------

\section{Results}
