% ---------------------------------------------------------------------------
% Introduction
%
% STRUCTURE CHANGED 2026-08-18, second cut. The old chain had ten argued links
% and could not fit 2.5 pages; the user's instruction was to carry the argument
% only as far as agent-maintainability, then pivot straight to "so we were
% curious how HL does on adversarial games", the benchmark, the classic RL
% environments, the low-K / low-sample finding, and the three contributions.
% Six argued links were therefore DELETED, not compressed. Where each went is
% recorded at the bottom of this comment; every one of them is now owed by
% another section and appears nowhere in the body until that section is written.
%
% THE PAPER'S LOGIC IN FIVE MOVES, as the user stated it 2026-08-19. This is the
% canonical version; the paragraph chain below is how Section 1 realizes moves 1-2
% and previews 3-5. If the two ever disagree, this block wins.
%   1. Background and problem. The symbolic paradigm was hard to maintain; agents
%      change that; we propose HL.  -> paragraphs 1-3.
%   2. Purpose. Measure how far an agent can carry a symbolic policy under
%      continued maintenance.  -> paragraph 3's closing question, "how high is the
%      ceiling?".
%   3. Design and result. A venue spanning 30 years, whose policies are
%      predominantly symbolic; the agent reaches SOTA on essentially all of it.
%      -> Sections 4 and 5.
%   4. Comparison and analysis. Against RL, in RL's own environments, on sample
%      efficiency, ROBUSTNESS, and breadth.  -> Section 5.
%   5. Theory. The same facts read through Kolmogorov complexity.  -> Section 6.
%
% TWO MISMATCHES BETWEEN THAT CHAIN AND THE DRAFT, both open as of 2026-08-19 and
% neither one resolvable by editing prose:
%
%   (a) STRENGTH OF THE SOTA CLAIM. Move 3 says the agent reaches SOTA on
%       essentially all games. The draft says \val{nbeat} of \val{ngames} and
%       hedges with "strongest exactly where a budget binds". Those are different
%       papers. If \val{nbeat} really is at or near \val{ngames}, the hedge is
%       costing us the headline and the framing should be inverted: the result
%       becomes "the ceiling is above the human field, and here is the one game
%       where it is not", with the exceptions carrying the interest. If instead it
%       is a genuine subset, "基本都能" overstates it and the draft is already
%       right. DO NOT rewrite the framing until \val{nbeat} is measured -- writing
%       the strong version first and discovering a subset is how a paper acquires
%       a claim its own results contradict.
%       Related and separate: "reaches SOTA" and "overtakes SOTA" are different
%       claims (matching the best recorded human vs beating them). The draft says
%       overtakes, in the abstract, paragraph 5, and contribution 3. Keep one verb
%       across all three; if the result is parity on most games and wins on a few,
%       that is a third framing again and needs stating as such.
%
%   (b) ROBUSTNESS HAS NO HOME. Move 4 names it as one of three analysis axes, and
%       checked 2026-08-19 the word appears nowhere in the paper -- not in the body,
%       not in any scope comment, and no \val key measures it. Sample efficiency has
%       \val{effratio} and \val{crossover}; breadth has \val{ntransfer}; robustness
%       has nothing. From the user's earlier framing ("低K 和低 sampling 数量时 HL 的
%       效果和鲁棒性") it means stability of the result when the budget is small and
%       the policy short -- variance across seeds, games, and models, not a separate
%       experiment. It needs a key and a place in Section 5 before move 4 is
%       honestly delivered; \val{robustspread} is registered in numbers.tex for it.
%
% The chain as it now stands. Each paragraph exists to make the next necessary:
%
%   1. The historical arc, rewritten 2026-08-18 at the user's direction: symbolic
%      construction won adversarial games FIRST (deepblue2002), RL then displaced
%      it on the same terrain, and what separates the paradigms is how each one
%      improves -- a learned policy by continuing to play, a symbolic one only when
%      somebody revises it. The paragraph must land on maintenance cost as the
%      thing that bounded the paradigm, explicitly not a limit on what a program
%      can express; that is the whole point of the paragraph and the setup for
%      link 2. Two prohibitions from the same instruction, both violated by the
%      draft it replaced: do not attribute the displacement to what people
%      "generally thought" or "judged", and keep the register formal -- no
%      colloquial framing of the symbolic paradigm as looked down on. State the
%      historical record and the mechanism, not received opinion.
%      deepblue2002 is cited for the historical fact ONLY. It is not evidence
%      about maintenance cost, and its evaluation function was tuned partly from
%      grandmaster games, so it is not a pure hand-authoring datum either. See its
%      refs.bib annotation before extending the claim.
%   2. Relocate the cost: it is a property of the author, not the artifact.
%      Conceded, never refuted -- "everyone was wrong" is both weaker and less
%      true than "everyone was measuring the author". Ends by placing the paper on
%      RL's strongest terrain, which is why the comparison is not rigged.
%      Do not write that "nothing caps what a program can express", which this
%      paragraph said until 2026-08-18: the user judged it too absolute, and it is
%      also a weaker claim than the one available. The claim now made is
%      comparative and defensible -- per unit of construction the symbolic form is
%      if anything MORE expressive, since one conditional or one search states a
%      decision boundary many neurons only approximate. Keep it comparative; an
%      unbounded-expressiveness claim invites a referee to reach for universal
%      approximation and win.
%   3. Coding agents change who the author is. The operative property is
%      editability, not interpretability. This is the paper's hinge; without it
%      the paper reads as a defence of a paradigm already judged.
%      It must END ON THE QUESTION, as of 2026-08-18: the coding agent becomes the
%      optimizer that drives revision of the symbolic program, in place of the human
%      whose cost bounded the paradigm, and then the paragraph asks how high the
%      ceiling is. Before that it ended on the role of the property, which stated a
%      reframing and never asked anything, so the paper's motivating question was
%      never posed -- the next paragraph simply began answering it.
%      Keep the system NAMES here and wherever ECHO reappears. Chengpeng Hu
%      (diprl2026) and Michael Y. Hu (echo2025hindsight) are different people and
%      natbib renders both as "Hu et al." with only the year between them, so the
%      two read as one group's pair. Naming the systems disambiguates them; do not
%      revert either to "a recent system". Found by reading the rendered intro end
%      to end -- no mechanical check catches it.
%   4. HL itself -- the loop, its narrowness, and why attribution is the point.
%      Carries the vibecoding2025tournament contrast, including the concession
%      that their agents were NOT one-shot (most revised, all debugged); the
%      distinction is what their loop optimized, compilability rather than
%      standing. Dropping that concession misrepresents the paper we lean on
%      hardest.
%   5. Where we test it AND what we find, merged into one paragraph on 2026-08-18
%      at the user's direction (the two ran long as a pair). It carries the
%      archive, the folk ordering it refutes, the classic RL environments, and then
%      the results. The join is the sample-efficiency result's last clause --
%      "largest margins on games whose winning policy is short" -- which is what
%      makes the axis follow from a finding instead of being announced. Keep that
%      hinge if the paragraph is ever restructured; without it the description
%      length arrives unmotivated.
%      This is also now the ONLY place the two bounds appear. It states
%      the sample-efficiency result without arguing it (-> Section 3) and the
%      plateau without arguing it (-> Section 7), then does the one thing that
%      cannot be delegated: names the axis. Go kills rule complexity as the
%      x-axis; K(pi_good) is the axis, always. A game defeats the loop because its
%      *winning policy* is long, not because its rulebook is. Never write that a
%      hard game "starts near the capacity" -- the program grows from a naive draft
%      in every game and the capacity is fixed by the agent; what varies is where
%      K(pi_good) sits relative to it. Getting that backwards makes the central
%      inequality read as though the game moved the ceiling.
%      The signature must stay attached to the inequality: convergent description
%      length at the plateau, divergent Elo there, and it is the PAIR that
%      diagnoses. Convergent length alone proves nothing -- it is equally
%      consistent with games that never differed in complexity.
%   6. The two conceded objections (pretraining asymmetry, the archive's RL
%      scarcity confounded with contestant cost structure). Deliberately kept in
%      the intro across two rounds of cutting: a reader who first meets the
%      pretraining objection in Section 6 has already decided we were hiding it.
%   7. Contributions -- three bullets, introduced by one sentence rather than a
%      heading, each opening on "We define / We introduce / We compare" at the
%      user's direction 2026-08-18. They were 21 lines of re-argued motivation
%      before the cuts; the motivation is upstream now, so never let the bullets
%      re-derive it. The third bullet ends on practical guidelines for applying HL,
%      which is a promise Section 6 has to keep -- when HL pays and when a learned
%      method is the better spend. Nothing in the paper delivers that yet.
%
% NO \paragraph HEADINGS IN THIS SECTION. Links 3-6 carried run-in bold headings
% ("What coding agents changed.", "Heuristic learning.", "Where we test it.",
% "What we find.") until 2026-08-18, when the user removed them: a summary in
% front of each paragraph breaks the read. The argument is a single chain and has
% to run as continuous prose, so each paragraph now opens on its own first
% sentence. If a heading is ever re-added, link 3's opening breaks -- "They change
% who the author is" took its antecedent from the deleted heading and now reads
% "Coding agents change who the author is". 07-related-work.tex keeps its
% \paragraph headings on purpose; a related-work section is scanned by topic, and
% that is a different requirement from this one.
%
% Do not write "if-else". Characterizing what the winning programs contain is a
% result, not a punchline. Do not claim editability as our coinage; programmatic
% RL already calls its policies human-editable, and what changes is the role the
% property plays.
%
% Four things to hold onto while editing:
%   - The objection is conceded, never refuted. "Everyone was wrong" is both
%     weaker and less true than "everyone was measuring the author".
%   - Paragraphs 5 and 6 stay adjacent. Split them and the paper reads as two
%     unrelated findings instead of one bound with two sides.
%   - Do not write "if-else". Characterizing what the winning programs contain
%     is a result, not a punchline.
%   - Do not claim editability as our coinage. Programmatic RL already calls
%     its policies human-editable; what changes here is the role the property
%     plays -- precondition for the optimizer, not feature of the deliverable.
%   - In paragraph 6, never let rule complexity back in as the axis. An earlier
%     draft rejected it with the Go example and then wrote "rule complexity and
%     revision count are the same axis seen twice" three sentences later, which
%     contradicts the paragraph's own thesis. The axis is K(pi_good), always.
%     A game defeats the loop because its *winning policy* is long, not because
%     its rulebook is.
%     Related, and also drafted wrong once: do not write that a hard game
%     "starts near the capacity". The program grows from a naive draft in every
%     game; the capacity is fixed by the agent. What varies across games is where
%     K(pi_good) sits relative to that ceiling -- below it, the program reaches
%     winning strength before it becomes unmaintainable; above it, the program
%     stops improving while still short of winning. Getting this backwards makes
%     the paper's central inequality read as though the game moved the ceiling.
%
% LENGTH. HARD BUDGET: 2.5 pages, set by the user 2026-08-18 ("introduction
% 只能写到两页半"), reaffirmed the same day as "I want it to END at 2.5 pages".
% This section was 4.5 pages when the instruction came, 2.63 after the first cut,
% and is now 76 rendered lines = 1.49 pages of text spanning 2.3 pages physically
% (page 1 line 26 to page 3 line 13). Comfortably inside the budget with room to
% restore something if a referee asks for it -- and if room is spent, spend it on
% the upper-bound mechanism (link 6 of the old chain), which is the deletion that
% cost the most: the paper now asserts a maintenance-capacity limit in the
% introduction without ever saying in the introduction what maintenance capacity
% means. 07-related-work.tex owes that argument.
%
% HOW TO MEASURE IT, because getting this wrong wasted most of one session:
%   - Contributions is a \subsection* of the Introduction and counts INSIDE the
%     budget. Measuring the body alone gave 2.55 and looked compliant while the
%     real figure was 3.0.
%   - 51 non-blank lines fill a full page here; page 1 holds only 39 because the
%     title and abstract sit above. So text-volume (lines/51) and physical span
%     are DIFFERENT numbers -- 2.56 vs 2.63 right now. Report both.
%   - Count with: pdftotext -f N -l N build/main.pdf - | awk 'NF && !/^[0-9]+$/'
%     Blank lines, page numbers, and form feeds otherwise inflate it; an early
%     count of "141 lines" was pure artifact and did not move across three edits.
%   - Page 4's trailing "Background/Methodology/..." lines are empty section
%     headings stacked up, not content. Find the end marker, do not count to EOF.
%
% WHAT ACTUALLY REDUCES LENGTH: deleting whole sentences and clauses. Rewriting
% a sentence more tightly does NOT -- the line count sat at exactly 131 across
% six consecutive rewrites, and at 139 across three before that, because
% substituted text reflows to the same length. Do not repeat that; if a pass does
% not change the number, the pass was not a cut.
%
% WHERE THE DELETED MATERIAL WENT. Each target section's scope comment names what
% it owes; the upper bound has since been written, the other two have not:
%   -> 03-heuristic-learning.tex: the entire lower-bound argument (information per
%      sample, the on/off-policy spectrum, cross-policy-class reuse, rules as a
%      zero-sample prior) and the ECHO concession that the gain belongs to the
%      reader rather than the symbolic form; plus the model-based-RL baseline
%      rationale from the first cut. Keys offpolicyvaluellm2026, samplereuse2022,
%      echo2025hindsight, kuang2025generative live or die there. NOT WRITTEN YET.
%   -> 07-related-work.tex: the entire upper-bound argument. WRITTEN 2026-08-18,
%      in that section's "Long-horizon degradation and maintenance capacity"
%      paragraphs, which now carry the mechanism (accumulation defeats placement),
%      the three supporting results with their figures, the
%      replacement-vs-accumulation boundary, frontiereng2026's exponents, the "why
%      still open" argument, and selfimprovement2026survey. That stretch is
%      load-bearing for THIS file's capacity claim -- if it is ever cut back to a
%      survey paragraph, the introduction asserts a maintenance-capacity limit that
%      the paper never defines.
%   -> 02-preliminary.tex: the benchmark provenance argument, and the definitional
%      split the ECHO concession rests on -- lower bound over what the loop can
%      READ, upper over what it can EDIT.
% Never cut from THIS file: the Go example (it is what kills rule complexity as
% the axis), the single inequality with its signature, the not-one-shot concession
% about vibecoding2025tournament, and the two conceded objections.
%
% WHAT NOT TO TRY. Rewriting a sentence more tightly does not shorten the render;
% see the measurement note above. Both times this section came down it was by
% deleting whole paragraphs.
% ---------------------------------------------------------------------------

\section{Introduction}
\label{sec:introduction}

Coding agents have made heuristic learning a viable form of self-evolution: an agent
can read an executable program, interpret execution feedback, and revise the program
itself. This connects heuristic learning to the broader study of self-evolving agents,
program search, and algorithmic design, where recent systems have begun to explore
iterative policy or algorithm revision \citep{garnier2026heuristic,evopolicygym2026,
ai4aibench2026}. The attraction is that the artifact remains readable and editable,
while the labour of incorporating new experience can be delegated to an agent.

This opportunity also reopens the contrast between symbolic policies and reinforcement
learning (RL). Symbolic construction defeated strong human players through explicit
rules, evaluation functions, and search \citep{deepblue2002}; RL later surpassed
hand-written programs through continued interaction and self-play
\citep{alphazero2018,alphastar2019,openaifive2019}. The two paradigms therefore
improve differently: RL updates parameters from repeated experience, whereas a
symbolic policy changes only when its rules are revised. The latter is compact,
interpretable, and locally editable, but traditionally requires sustained human
maintenance. RL avoids that manual labour, yet its advantage is less secure when
opponents change sharply: non-stationarity, distribution shift, stale replay data,
and conflicting behavioural modes can all impair generalization
\citep{hernandezleal2019survey,zhang2018overfitting,cobbe2019quantifying,
gleave2021adversarial,eysenbach2019diversity,yu2020metaworld}.

Adversarial games are therefore a particularly revealing setting for heuristic
learning. Different opponents induce different effective environments, while each
opponent pattern is observed through only a limited number of samples. At the same
time, a game replay exposes states, actions, opponent behaviour, and outcomes that can
be mapped to concrete program edits. Existing heuristic-learning studies do not yet
provide a systematic evaluation in this regime: they primarily use single-agent
environments, fixed evaluators, or algorithm-level rather than policy-level revision.
This paper asks how far an agent can maintain a symbolic policy when the feedback is
adversarial and the policy must face a changing field.

% SCOPE, added 2026-08-19 at the user's direction, then cut to two sentences the same day
% ("就说我们在博弈类上面打，就行"). It belongs in this paragraph rather than its own
% because the scope is a condition on the question, not a topic. Do not re-expand it into
% an argument about what each requirement supplies the loop -- that draft existed and was
% cut. The one thing that must survive: games are the paradigm's favourable case, stated
% plainly. A referee will look for whether we know that, and it costs one clause.

Coding agents change who the author is. Once the author is a machine, the property
that governs whether revision can continue is \emph{editability} rather than
interpretability: interpretability asks whether a
human understands the policy, editability whether the optimization loop can act on
the artifact at all. A symbolic program can be read in full and changed in part, and
takes the heterogeneous inputs maintenance requires --- rules, its own source,
replays of losses. A learned policy admits no comparable loop: its behaviour lives in
weights that cannot be edited locally and offer no handle by which a specific failure
traces to a specific parameter. The two properties do come apart --- a system can
reason in natural language about every decision and still hold its policy in weights
\citep{tig2025think} --- and programmatic RL already calls its policies human-readable
and editable \citep[DiPRL;][]{diprl2026}. What changes here is the role the property
plays: not a feature of the delivered artifact, but the reason an optimizer can exist
for it at all. Once a coding agent is that optimizer --- the engine driving revision
of a symbolic program, in place of the human whose cost bounded the paradigm --- the
question the paradigm was retired on becomes askable again, and quantitatively: how
high is the ceiling?

We study it through \emph{adversarial-game heuristic learning} (HL): a coding agent maintains a heuristic program
by reading the rules, its own source, and replays of its losses, then rescores it
against a frozen opponent pool. The loop is deliberately narrow --- one revision per
iteration, every version and score kept in an append-only log --- because attribution
is the property we are trying to preserve: without it a plateau can be observed but
not diagnosed. A stronger model alone does not supply that. In the closest existing
head-to-head humans took the top five places, and a frontier model handed the winning
student's program dropped it to tenth \citep{vibecoding2025tournament}; those agents
were not one-shot --- most were revised and all debugged over many cycles --- but the
loop optimized compilability, not tournament standing. The missing term is revisions
carrying an adversarial signal.

Figure~\ref{fig:hl-arena-overview} previews the object we measure: one executable policy
is revised from game feedback and then placed against a fixed field of opponents. The
lower row shows the heterogeneous contest games that make the comparison a test of
adversarial-game heuristic learning rather than a claim about HL in general.

\begin{figure}[t]
\centering
\begin{tikzpicture}[x=1cm,y=1cm,>=Stealth,
  box/.style={draw,rounded corners=2pt,fill=blue!4,align=center,
              minimum height=0.62cm,inner sep=3pt,font=\scriptsize},
  game/.style={draw,rounded corners=2pt,fill=gray!8,align=center,
               minimum height=0.42cm,inner sep=2pt,font=\scriptsize}]
  \node[box,minimum width=2.25cm] (ctx) at (1.35,1.05)
    {rules + source\\loss replays};
  \node[box,minimum width=2.25cm] (agent) at (4.1,1.05)
    {generative\\agent};
  \node[box,minimum width=2.25cm] (pol) at (6.85,1.05)
    {revised executable\\policy};
  \node[box,minimum width=2.25cm] (eval) at (9.6,1.05)
    {frozen opponent\\pool / Elo};
  \draw[->] (ctx) -- node[above,font=\scriptsize]{read} (agent);
  \draw[->] (agent) -- node[above,font=\scriptsize]{edit} (pol);
  \draw[->] (pol) -- node[above,font=\scriptsize]{rescore} (eval);
  \draw[->,rounded corners] (eval.north) -- ++(0,0.35) -| node[pos=.65,above,font=\scriptsize]{score + trace} (agent.north);
  \node[font=\scriptsize\bfseries,anchor=west] at (0,0.35) {AHL-Arena: adversarial-game suite};
  \node[game,minimum width=1.55cm] at (1.15,-0.15) {Miracle};
  \node[game,minimum width=1.55cm] at (2.85,-0.15) {AquaWar};
  \node[game,minimum width=1.55cm] at (4.55,-0.15) {LostSpace};
  \node[game,minimum width=1.55cm] at (6.25,-0.15) {SnakeGo};
  \node[game,minimum width=1.55cm] at (7.95,-0.15) {AntWar};
  \node[game,minimum width=1.55cm] at (9.65,-0.15) {Generals};
  \node[game,minimum width=1.55cm] at (1.75,-0.72) {Rollman};
  \node[game,minimum width=1.55cm] at (4.05,-0.72) {DeepClue};
  \node[game,minimum width=1.55cm] at (6.35,-0.72) {AntWar2};
\end{tikzpicture}
\caption{Overview of heuristic learning and \benchname. The upper row is the single-artifact
revision loop; the lower row lists the contest games used as its adversarial-game setting.
This is a structural placeholder for the final overview figure.}
\label{fig:hl-arena-overview}
\end{figure}

Measuring a ceiling needs a domain where many independent authors already pushed the
symbolic paradigm as far as a real budget allowed, with the results recorded, so we
build \benchname on the complete archive of a collegiate game AI contest running
since \val{founded}:
games authored for the contest itself, \val{nsub} submissions, and \val{nmatch}
adjudicated matches, written under deadline with standings at stake. That provenance
% Repointed 2026-08-20 from sec:preliminary, deleted that day. This sentence is about what
% the archive SUPPLIES -- contamination resistance and a distribution to place a method
% within -- and both are now argued in 4.1 (provenance, and the finalist-field caveat) with
% the percentile defined in 4.3. Provenance is the closer match for the sentence as written.
supplies contamination resistance and a \emph{distribution} of human skill to place a
method within (Section~\ref{sec:arena-provenance}), and it records a folk ordering --- rules
for beginners, search for the intermediate, RL as the highest ceiling --- that gets
the asymptotic ceilings right and still fails to predict who wins: \val{pheur} of
prize-winning programs contain no learned component, and RL, present in \val{prl} of
submissions, reached the finals in \val{nrlfinal} of \val{editions} editions. Running
HL there and in the classic RL environments, where learned baselines are established,
we find it strongest exactly where a budget binds --- on \val{nbeat} of
\val{ngames} games it overtakes the strongest submission the archive records, a
program refined by contestants over \val{humanspan} of competition, which places
the question of the ceiling above the human field rather than within it; the
\val{pct}th percentile across the field, \val{effratio} the sample efficiency of
model-based RL at matched Elo, and the largest margins on games whose winning
policy is short. That last
regularity names the axis, which is a description length and not the length of the
rules: Go's rules fit on a page, yet no compact symbolic policy plays it well. What
governs the outcome is the description length of a \emph{symbolic} policy good enough
to win, written $K(\pi_{\mathrm{good}})$ after Kolmogorov complexity and estimated by
compression, the quantity itself being uncomputable
\citep{lividanyi2019kolmogorov,rissanen1978mdl}, and the prediction is a single
inequality: HL succeeds while $K(\pi_{\mathrm{good}})$ stays below the agent's
maintenance capacity and plateaus as it approaches it. Because that capacity belongs
to the agent rather than the game, the inequality predicts a signature we then
observe --- description length at the plateau converges across games of differing
complexity while the Elo reached there does not, and it is the pairing, not either
half, that separates a limit of the maintainer from a limit of the game
(Section~\ref{sec:discussion}). Neither paradigm dominates: model-based RL overtakes
HL beyond \val{crossover} environment steps, so the budget and the game's structure
decide, while the loop itself transfers to \val{ntransfer} further benchmarks without
retuning.

Two objections are conceded here and answered in Section~\ref{sec:discussion}. The
agent arrives with a pretraining corpus while RL starts cold, and no denominator makes
that symmetric; we report environment steps as what is scarce in a new domain and test
what pretraining supplies (Section~\ref{sec:benchmark}). And the archive's RL
scarcity is confounded with its participants' cost structure --- students on a deadline
have limited compute, time, and RL expertise --- so it records what wins under contest
constraints. Every comparison here is budget-conditional.

% Method, benchmark, comparison -- in that order, so the algorithm is the paper's
% subject and the benchmark is what it is measured on. The archive audit sits
% under the benchmark: it describes what the archive contains, and is not a
% result of comparing HL against RL.
% The \subsection*{Contributions} heading was removed 2026-08-18: the list is
% introduced in a sentence instead, for the same reason the run-in paragraph
% headings went. Do not reinstate it.
Our contributions are threefold.

\begin{itemize}[leftmargin=*]
  \item \textbf{We define Adversarial Heuristic Learning (AHL)}: an agent maintains
  one executable symbolic policy under adversarial feedback. Each iteration makes
  one revision and logs the version, score, and trace, making improvement and
  maintenance plateaus measurable rather than anecdotal (Section~\ref{sec:benchmark}).

  \item \textbf{We introduce \benchname}, an archive drawn from a real competition
  rather than built for evaluation. It supplies human programs and matches, classic
  game controls, frozen opponent pools, cross-edition Elo, and an audit of learned
  components, providing a reproducible human field for adversarial evaluation.

  \item \textbf{We compare adversarial-game HL with human and RL baselines} under
  matched interaction budgets. We report performance, sample efficiency, and the
  maintenance-capacity signature, then identify the budget regimes in which an
  editable policy pays off and those in which a learned policy is the better spend
  (Section~\ref{sec:discussion}).
\end{itemize}
